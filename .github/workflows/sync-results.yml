name: Sync WC2026 results
on:
  # Wind-down 2026-07-14: group stage ended 27 Jun; the every-30-min cron was
  # retired (it only syncs GROUP scores). Manual dispatch kept just in case.
  workflow_dispatch: {}        # manual "Run workflow" button
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger sync-results edge function
        run: |
          curl -fsS -X POST "https://voqwpmmgrvalavvmzrih.supabase.co/functions/v1/sync-results" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZvcXdwbW1ncnZhbGF2dm16cmloIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODA0NTI0NjgsImV4cCI6MjA5NjAyODQ2OH0.t0kROq7-8CgwKpJmymKsOtvANHIztwhSC9u8ab4rTVM" \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}"
